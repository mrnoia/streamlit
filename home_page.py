import streamlit as st


def index(selected):
    st.title(f"You have selected {selected}")
    st.write(
        """
        Learning how to use Streamlit and how to integrate other Python library with it 
        """
    )
