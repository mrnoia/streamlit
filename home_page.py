import streamlit as st


def index(selected):
    st.title(f"You have selected {selected}")
    st.write(
        """
        Learning to use Strealit and to integrate other Python library with it 
        """
    )
